<!DOCTYPE html>
<html>
<head>
    <title>Zone Controller</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .zone-container {
            max-width: 800px;
            margin: 0 auto;
        }
        .zone-card {
            border: 1px solid #ddd;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 8px;
        }
        .zone-controls {
            display: flex;
            gap: 10px;
            margin-top: 10px;
        }
        button {
            padding: 8px 16px;
            border-radius: 4px;
            border: none;
            background-color: #007AFF;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="zone-container">
        <h1>Zone Controller</h1>
        
        #for(zone in zones):
            <div class="zone-card">
                <h2>#(zone.name)</h2>
                <p>Status: #(zone.status)</p>
                <div class="zone-controls">
                    <form method="POST" action="/zones/#(zone.id)/toggle">
                        <button type="submit">
                            #if(zone.status == "on"):
                                Turn Off
                            #else:
                                Turn On
                            #endif
                        </button>
                    </form>
                    <form method="POST" action="/zones/#(zone.id)/volume">
                        <input type="range" min="0" max="100" value="#(zone.volume)" name="volume">
                        <button type="submit">Set Volume</button>
                    </form>
                </div>
            </div>
        #endfor
    </div>
</body>
</html>
ap
